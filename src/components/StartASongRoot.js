import React, { Component } from 'react';
import Button from '@material-ui/core/Button';

class StartASongRoot extends Component {
  render() {
    return (
      <div>
        <Button>Randomly Pick One</Button>
      </div>
    );
  }
}

export default StartASongRoot;
